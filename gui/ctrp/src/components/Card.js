import styled from 'styled-components'

export const mainContentTitleFormat = styled.h1`
    text-transform:capitalize;
    font-size:large;
    border:0.05rem solid var(--cardTitleColor);
    color: var(--cardTitleColor);
    border-radius: 0.5rem;
    padding: 0.2rem 0.5rem;
    cursor:pointer;
    margin:0.2rem 0.5rem 0;
`;

export const subtitleContentFormat = styled.h2`
    text-transform:capitalize;
    font-size:medium;
    background: transparent;
    border:0.05rem solid var(--cardTitleColor);
    color: var(--cardTitleColor);
    border-radius: 0.5rem;
    padding: 0.2rem 0.5rem;
    cursor:pointer;
    margin:0.2rem 0.5rem 0;
`;


export const contentTitleFormat = styled.h1`
    text-transform:capitalize;
    font-size:1.4rem;
    background: transparent;
    border:0.05rem solid var(--cardTitleColor);
    color: var(--cardTitleColor);
    border-radius: 0.5rem;
    padding: 0.2rem 0.5rem;
    cursor:pointer;
    margin:0.2rem 0.5rem 0;
`;

export const contentLinkForwardFormat = styled.a`
    text-transform:capitalize;
    font-size:1.4rem;
    background: transparent;
    border:0.05rem solid var(--cardTitleColor);
    color: var(--cardTitleColor);
    border-radius: 0.5rem;
    padding: 0.2rem 0.5rem;
    cursor:pointer;
    margin:0.2rem 0.5rem 0;
`;

export const contentCreatorFormat = styled.h4`
    text-transform:capitalize;
    font-size:1.4rem;
    background: transparent;
    border:0.05rem solid var(--cardTitleColor);
    color: var(--cardTitleColor);
    border-radius: 0.5rem;
    padding: 0.2rem 0.5rem;
    cursor:pointer;
    margin:0.2rem 0.5rem 0;
`;


export const contentFormat = styled.text`
    text-transform:capitalize;
    font-size:1.4rem;
    background: transparent;
    border:0.05rem solid var(--cardTitleColor);
    color: var(--cardTitleColor);
    border-radius: 0.5rem;
    padding: 0.2rem 0.5rem;
    cursor:pointer;
    margin:0.2rem 0.5rem 0;
`;

export const contentLinkFormat = styled.a`
    text-transform:capitalize;
    font-size:1.4rem;
    background: transparent;
    border:0.05rem solid var(--cardTitleColor);
    color: var(--cardTitleColor);
    border-radius: 0.5rem;
    padding: 0.2rem 0.5rem;
    cursor:pointer;
    margin:0.2rem 0.5rem 0;
`;
